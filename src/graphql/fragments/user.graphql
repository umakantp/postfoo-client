fragment MyUserResponse on User {
  id
  firstName
  lastName
  name
  mobile
  token
  status
  isVerified
  isBlocked
  memberships {
    id
    role
    portfolio {
      id
      name
    }
  }
}

fragment GuestUserResponse on User {
  id
}
